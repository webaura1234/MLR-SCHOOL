/**
 * Google Ads global site tag — use GoogleTagLoader in layout (shared with GA4).
 * Re-exported for clarity when documenting Ads-specific setup.
 */
export { default } from '@/components/GoogleTagLoader';
